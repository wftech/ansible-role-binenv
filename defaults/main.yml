
binenv_download_url: ~
# https://github.com/devops-works/binenv/releases/latest/download/binenv_linux_amd64


# add binenv setup to /etc/profile so it is available to all users
binenv_do_autoinstall: true
